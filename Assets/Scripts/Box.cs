using UnityEngine;

public class Box : MonoBehaviour {
    // Configuration
    public BoxColor Color;
    public Color[] Colors;

    void Start() {
        // var sr = GetComponent<SpriteRenderer>();
        // sr.color = Colors[(int)Color];
    }
}